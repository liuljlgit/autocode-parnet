package ${inftCachePackagePath};

import com.cloud.ftl.ftlbasic.service.IBaseCache;
import ${entityPackagePath}.${className};

/**
 * I${className}Cache cache接口类
 * @author lijun
 */
public interface I${className}Cache extends IBaseCache<${className}>{

}
