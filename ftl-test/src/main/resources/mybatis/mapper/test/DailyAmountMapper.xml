<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.ftl.ftltest.test.dao.IDailyAmountDao">
    <resultMap id="BaseResultMap" type="com.cloud.ftl.ftltest.test.entity.DailyAmount">
        <id column="da_id" jdbcType="BIGINT" property="daId" />
        <result column="entity_id" jdbcType="INTEGER" property="entityId" />
        <result column="date_time" jdbcType="TIMESTAMP" property="dateTime" />
        <result column="sett_profit" jdbcType="DECIMAL" property="settProfit" />
        <result column="expend_profit" jdbcType="DECIMAL" property="expendProfit" />
        <result column="income_profit" jdbcType="DECIMAL" property="incomeProfit" />
        <result column="profit_hours" jdbcType="TINYINT" property="profitHours" />
        <result column="deficit_hours" jdbcType="TINYINT" property="deficitHours" />
        <result column="minus_deviation" jdbcType="DECIMAL" property="minusDeviation" />
        <result column="positive_deviation" jdbcType="DECIMAL" property="positiveDeviation" />
        <result column="status" jdbcType="TINYINT" property="status" />
        <result column="ce" jdbcType="TINYINT" property="ce" />
        <result column="sa" jdbcType="TINYINT" property="sa" />
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
        <result column="status_time" jdbcType="TIMESTAMP" property="statusTime" />
    </resultMap>

    <sql id="Base_Column_List">
        da_id,entity_id,date_time,sett_profit,expend_profit,income_profit,profit_hours,deficit_hours,minus_deviation,positive_deviation,status,ce,sa,create_time,status_time
    </sql>

    <sql id="Base_Object_List">
        #{at.daId},#{at.entityId},#{at.dateTime},#{at.settProfit},#{at.expendProfit},#{at.incomeProfit},#{at.profitHours},#{at.deficitHours},#{at.minusDeviation},#{at.positiveDeviation},#{at.status},#{at.ce},#{at.sa},#{at.createTime},#{at.statusTime}
    </sql>

    <sql id="Set_Not_Null_List">
        <set>
            <if test="ut.daId != null">
                da_id = #{ut.daId},
            </if>
            <if test="ut.entityId != null">
                entity_id = #{ut.entityId},
            </if>
            <if test="ut.dateTime != null">
                date_time = #{ut.dateTime},
            </if>
            <if test="ut.settProfit != null">
                sett_profit = #{ut.settProfit},
            </if>
            <if test="ut.expendProfit != null">
                expend_profit = #{ut.expendProfit},
            </if>
            <if test="ut.incomeProfit != null">
                income_profit = #{ut.incomeProfit},
            </if>
            <if test="ut.profitHours != null">
                profit_hours = #{ut.profitHours},
            </if>
            <if test="ut.deficitHours != null">
                deficit_hours = #{ut.deficitHours},
            </if>
            <if test="ut.minusDeviation != null">
                minus_deviation = #{ut.minusDeviation},
            </if>
            <if test="ut.positiveDeviation != null">
                positive_deviation = #{ut.positiveDeviation},
            </if>
            <if test="ut.status != null">
                status = #{ut.status},
            </if>
            <if test="ut.ce != null">
                ce = #{ut.ce},
            </if>
            <if test="ut.sa != null">
                sa = #{ut.sa},
            </if>
            <if test="ut.createTime != null">
                create_time = #{ut.createTime},
            </if>
            <if test="ut.statusTime != null">
                status_time = #{ut.statusTime}
            </if>
        </set>
    </sql>

    <sql id="Set_With_Null_List">
        <set>
            da_id = #{ut.daId},
            entity_id = #{ut.entityId},
            date_time = #{ut.dateTime},
            sett_profit = #{ut.settProfit},
            expend_profit = #{ut.expendProfit},
            income_profit = #{ut.incomeProfit},
            profit_hours = #{ut.profitHours},
            deficit_hours = #{ut.deficitHours},
            minus_deviation = #{ut.minusDeviation},
            positive_deviation = #{ut.positiveDeviation},
            status = #{ut.status},
            ce = #{ut.ce},
            sa = #{ut.sa},
            create_time = #{ut.createTime},
            status_time = #{ut.statusTime}
        </set>
    </sql>

    <sql id="where_sql">
        <if test="st.daId != null">
            AND da_id = #{st.daId}
        </if>
        <if test="st.entityId != null">
            AND entity_id = #{st.entityId}
        </if>
        <if test="st.dateTime != null">
            AND date_time = #{st.dateTime}
        </if>
        <if test="st.settProfit != null">
            AND sett_profit = #{st.settProfit}
        </if>
        <if test="st.expendProfit != null">
            AND expend_profit = #{st.expendProfit}
        </if>
        <if test="st.incomeProfit != null">
            AND income_profit = #{st.incomeProfit}
        </if>
        <if test="st.profitHours != null">
            AND profit_hours = #{st.profitHours}
        </if>
        <if test="st.deficitHours != null">
            AND deficit_hours = #{st.deficitHours}
        </if>
        <if test="st.minusDeviation != null">
            AND minus_deviation = #{st.minusDeviation}
        </if>
        <if test="st.positiveDeviation != null">
            AND positive_deviation = #{st.positiveDeviation}
        </if>
        <if test="st.status != null">
            AND status = #{st.status}
        </if>
        <if test="st.ce != null">
            AND ce = #{st.ce}
        </if>
        <if test="st.sa != null">
            AND sa = #{st.sa}
        </if>
        <if test="st.createTime != null">
            AND create_time = #{st.createTime}
        </if>
        <if test="st.statusTime != null">
            AND status_time = #{st.statusTime}
        </if>
        <if test="st.criterias != null">
            <foreach collection="st.criterias" item="criteria" separator=" ">
                <if test="criteria.valid">
                    ${criteria.opt}
                    <choose>
                        <when test="criteria.criterions.size() > 1">
                            <trim prefix="(" prefixOverrides="and|or" suffix=")">
                                <foreach collection="criteria.criterions" item="criterion">
                                    <choose>
                                        <when test="criterion.noValue">
                                            ${criterion.condition}
                                        </when>
                                        <when test="criterion.oneValue">
                                            ${criterion.condition} #{criterion.value1}
                                        </when>
                                        <when test="criterion.secondValue">
                                            ${criterion.condition} #{criterion.value1} and #{criterion.value2}
                                        </when>
                                        <when test="criterion.listValue">
                                            ${criterion.condition}
                                            <foreach collection="criterion.list" item="listItem" open="(" close=")" separator=",">
                                                #{listItem}
                                            </foreach>
                                        </when>
                                    </choose>
                                </foreach>
                            </trim>
                        </when>
                        <otherwise>
                            <trim prefixOverrides="and|or">
                                <foreach collection="criteria.criterions" item="criterion">
                                    <choose>
                                        <when test="criterion.noValue">
                                            ${criterion.condition}
                                        </when>
                                        <when test="criterion.oneValue">
                                            ${criterion.condition} #{criterion.value1}
                                        </when>
                                        <when test="criterion.secondValue">
                                            ${criterion.condition} #{criterion.value1} and #{criterion.value2}
                                        </when>
                                        <when test="criterion.listValue">
                                            ${criterion.condition}
                                            <foreach collection="criterion.list" item="listItem" open="(" close=")" separator=",">
                                                #{listItem}
                                            </foreach>
                                        </when>
                                    </choose>
                                </foreach>
                            </trim>
                        </otherwise>
                    </choose>
                </if>
            </foreach>
        </if>
    </sql>

    <!--获取最大的主键-->
    <select id="selectMaxId" resultType="java.lang.Long">
        select IFNULL(max(da_id), 0) from `daily_amount`
    </select>

    <!--根据主键获取对象-->
    <select id="selectById" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" /> from daily_amount
        <where>
            AND da_id = #{daId}
        </where>
    </select>

    <!--查询列表-->
    <select id="selectList" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" /> from daily_amount
        <where>
            <include refid="where_sql" />
        </where>
        <if test="st.orderByList!=null and st.orderByList.size() > 0">
            order by
            <foreach collection="st.orderByList" item="order" separator=",">
                ${order.field} ${order.mode}
            </foreach>
        </if>
        <if test="st.page != null and st.pageSize != null and st.page > 0" >
            limit #{st.index},#{st.pageSize}
        </if>
    </select>

    <!--查询列表-->
    <select id="selectFieldList" resultMap="BaseResultMap">
        select
        <foreach collection="fl" item="field" separator=",">
            ${field}
        </foreach>
        from daily_amount
        <where>
            <include refid="where_sql" />
        </where>
        <if test="st.orderByList!=null and st.orderByList.size() > 0">
            order by
            <foreach collection="st.orderByList" item="order" separator=",">
            ${order.field} ${order.mode}
            </foreach>
        </if>
        <if test="st.page != null and st.pageSize != null and st.page > 0" >
            limit #{st.index},#{st.pageSize}
        </if>
    </select>

    <!--查询列表总数-->
    <select id="selectCount" resultType="java.lang.Long">
        select count(1) from daily_amount
        <where>
            <include refid="where_sql" />
        </where>
    </select>

    <!--根据ID列表获取记录列表-->
    <select id="selectBatchIds" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" /> from daily_amount
        <where>
        da_id in
            <foreach collection="list" open="(" close=")" separator="," item="item">
            #{item}
            </foreach>
        </where>
    </select>

    <!--插入对象-->
    <insert id="add">
        insert into daily_amount(<include refid="Base_Column_List" />)
        values(<include refid="Base_Object_List" />)
    </insert>

    <!--批量插入对象-->
    <insert id="addBatch">
        insert into daily_amount(<include refid="Base_Column_List" />)
        values
        <foreach collection="list" index="index" item="at" separator=",">
            (<include refid="Base_Object_List" />)
        </foreach>
    </insert>

    <!--更新对象-->
    <update id="updateNotNull">
        update daily_amount
        <include refid="Set_Not_Null_List" />
        <where>
            AND da_id = #{ut.daId}
        </where>
    </update>

    <!--批量更新对象-->
    <update id="updateBatchNotNull">
        <foreach collection="list" separator=";" item="ut">
            update daily_amount
            <include refid="Set_Not_Null_List" />
            <where>
                AND da_id = #{ut.daId}
            </where>
        </foreach>
    </update>

    <!--更新对象(全更新)-->
    <update id="updateWithNull">
        update daily_amount
        <include refid="Set_With_Null_List" />
        <where>
            AND da_id = #{ut.daId}
        </where>
    </update>

    <!--批量更新对象(全更新)-->
    <update id="updateBatchWithNull">
        <foreach collection="list" separator=";" item="ut">
            update daily_amount
            <include refid="Set_With_Null_List" />
            <where>
                AND da_id = #{ut.daId}
            </where>
        </foreach>
    </update>

    <!--更新对象,根据查询条件更新-->
    <update id="updateByMap">
        update daily_amount
        <set>
            <foreach collection="um" index="key" item="value" separator=",">
                ${key} = #{value}
            </foreach>
        </set>
        <where>
            <include refid="where_sql" />
        </where>
    </update>

    <!--根据查询条件删除记录-->
    <delete id="delete">
        delete from daily_amount
        <where>
            <include refid="where_sql" />
        </where>
    </delete>

    <!--根据主键删除对象-->
    <delete id="deleteById">
        delete from daily_amount
        <where>
            AND da_id = #{daId}
        </where>
    </delete>

    <!-- 批量删除对象 -->
    <delete id="deleteBatchIds">
        delete from daily_amount
        <where>
            da_id in
            <foreach collection="list" open="(" close=")" separator="," item="item">
                #{item}
            </foreach>
        </where>
    </delete>


    <!--~~~~~~~~~~~~~~~~~~~~~~ custom code begin ~~~~~~~~~~~~~~~~~~~~~~-->
    
    <!--~~~~~~~~~~~~~~~~~~~~~~ custom code end ~~~~~~~~~~~~~~~~~~~~~~-->

</mapper>